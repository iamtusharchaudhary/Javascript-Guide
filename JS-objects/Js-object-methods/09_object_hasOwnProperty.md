```

const person = {
    name: "Punit",
    age: 33
}

const obj2 = Object.hasOwnProperty('name')  // This is true because it has name key
const obj3 = Object.hasOwnProperty('city')  // This is false because it doesn't have city key

console.log(obj2);
console.log(obj3);

```
